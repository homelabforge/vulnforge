name: Publish

on:
  push:
    tags: ['v*.*.*']

permissions:
  contents: read

jobs:
  publish:
    uses: homelabforge/shared-workflows/.github/workflows/python-react-publish.yml@v1.0.0
    with:
      enable-bootstrap-token: true
      security-tripwire-script: .github/scripts/security-tripwire.sh
      image-name: homelabforge/vulnforge
      release-name-prefix: 'VulnForge v'
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
